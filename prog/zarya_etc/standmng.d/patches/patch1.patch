#!/bin/bash

echo "Setting PAM for run_init..."
sed -i "/#%PAM/a auth       sufficient   pam_rootok.so" /etc/pam.d/run_init
#ips=(`standmng get allip`)
#for ((i=0; i<${#ips[@]}; i++))
#	do
#	ssh -T root@${ips[$i]} <<-EOF
#		sed -i "/#%PAM/a auth       sufficient   pam_rootok.so" /etc/pam.d/run_init
#		EOF
#	done

echo "Installing necessary packeges..."
yum -y -q install httpd php php-pdo expect netlabel_tools
#ips=(`standmng get allip`)
#for ((i=0; i<${#ips[@]}; i++))
#	do
#	ssh -T root@${ips[$i]} <<-EOF
#		yum -y -q install httpd php php-pdo expect netlabel_tools
#		EOF
#	done
